---
date: 2026-06-05
tags: [devlog, vue3, typescript, vite]
project: palmsugar-lab
aliases: ["2026-06-05 开发日志"]
---

# 2026-06-05 开发日志

> **项目**：[[README]]
> **关联**：[[PRD]] · [[DISCUSSION_ARCHIVE]]

## 一、今日完成

| # | 事项 | 涉及文件 | 备注 |
|---|------|----------|------|
| 1 | 单词卡 SRS 系统（简化版 SM-2 算法） | `src/composables/useSRS.ts`、[[Vocabulary.vue]] | 认识/模糊/不认识三档反馈，localStorage 持久化 |
| 2 | 练习题库升级（语法选择 + 翻译题） | `src/views/Exercise.vue`、`src/composables/useExerciseProgress.ts` | 新增题型支持、错题本、级别筛选 |
| 3 | 学习仪表盘主页重构 | `src/views/Japanese.vue` | 今日概览、快捷入口、学习动态 |
| 4 | 存储层封装 | `src/utils/storage.ts` | 为 uni-app 迁移预留 |
| 5 | 示例数据 & 数据模板 | `src/content/japanese/words/`、`src/content/japanese/exercise/`、[[DATA_TEMPLATE]] | 每级别 5~10 个示例单词/题目 |
| 6 | 项目规范 & 文档 | `AGENTS.md`、`docs/PRD.md`、[[DISCUSSION_ARCHIVE]] | 定义开发规范、产品需求、讨论留档 |

## 二、遇到的问题

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | `vue-tsc` 命令未找到 | node_modules 缺失 | 运行 `npm install` 安装依赖 |
| 2 | Exercise.vue 模板语法错误 | 多个 `<button>` 和 `<div>` 标签的 `>` 符号被错误换行 | 检查并修复所有标签闭合位置 |
| 3 | `exerciseList.value.length` 类型错误 | 模板中 Vue 自动解包 ref | 在模板中使用 `exerciseList.length` |
| 4 | `getExerciseById` 导入错误 | 从错误的 composable 导入 | 修正导入路径到 `api/exercise.ts` |
| 5 | 未使用变量/类型报错 | TypeScript `noUnusedLocals` 严格检查 | 移除未使用的 import |

## 三、关键决策

| # | 决策点 | 选项A | 选项B | 选择 | 理由 |
|---|--------|-------|-------|------|------|
| 1 | 复习算法 | 简化版 SM-2 | 自定义算法 | SM-2 | 经典可靠，实现简单，适合个人项目 |
| 2 | 数据持久化 | localStorage | IndexedDB | localStorage | 纯前端，数据量不大，方便调试 |
| 3 | App 方案 | uni-app | 独立原生 | uni-app（将来） | Vue 3 兼容，多端覆盖，迁移成本低 |
| 4 | 图表库 | ECharts | 本次不加 | 本次不加 | 保持轻量，后续迭代再加 |
| 5 | 数据加载 | import.meta.glob | 运行时请求 | import.meta.glob | 构建时加载，无网络依赖，适合静态站 |

## 四、下一步

- [ ] 补充更多真实单词和练习题数据
- [ ] 根据实际使用反馈调整 SM-2 算法参数
- [ ] 接入 ECharts 展示学习统计图表
- [ ] 用户测试反馈收集与优化
- [ ] uni-app 迁移准备（替换 `storage.ts` 实现）
